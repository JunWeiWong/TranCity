package Model;

/**
 * Recently the two transit system share nothing in common, so that the super class for all types of
 * transit system is empty at this point. However, this class can make the system more expandable.
 */
class TransitSystem {
}
